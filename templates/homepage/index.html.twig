{% extends 'base.html.twig' %}

{% block title %}Homepage - {{ parent() }}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    Latest movie :
    <a href="{{ path('app_movie_show', {'id': latest_movie.id}) }}">
        {{ latest_movie.title }}
    </a>
</div>
{% endblock %}
