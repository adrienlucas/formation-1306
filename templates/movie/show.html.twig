{% extends 'base.html.twig' %}

{# {% block title %}Show movie - {{ parent() }}{% endblock %} #}

{% block title 'Show movie - ' ~ parent() %}

{% block body %}
    <img src="{{ movie_poster }}" />
    
    <h2>{{ movie.title }}</h2>
    <p>{{ movie.description }}</p>
{% endblock %}
